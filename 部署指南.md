# 问卷网站部署指南

## 方式一：Vercel + Neon Postgres（推荐）

使用 PostgreSQL 关系型数据库，支持题目管理后台、CSV 导出、SQL 统计等高级功能。全程无需本地命令行操作。

### 总览

| 步骤 | 操作平台 | 用时 |
|------|----------|------|
| 1. 创建 Neon 数据库 | Neon 网站 | 2 分钟 |
| 2. 执行建表 SQL | Neon SQL Editor | 1 分钟 |
| 3. 部署到 Vercel | Vercel 网站 | 3 分钟 |
| 4. 配置环境变量 | Vercel 设置 | 2 分钟 |
| 5. 初始化题目数据 | 浏览器 / curl | 1 分钟 |

---

### 步骤 1：创建 Neon 数据库

1. 访问 https://neon.tech ，用 GitHub 登录，选择 **Free Plan**（免费）
2. 点击 **Create Project**
   - Project name：`gz-ebike-survey`
   - PostgreSQL 版本：选 14 或更高
   - Region：推荐 `Southeast Asia (Singapore)` 或 `US East (N. Virginia)`（离 Vercel 近）
3. 创建完成后，在 **Dashboard → Connection String** 区域复制完整连接串
   - 格式类似：`postgresql://user:password@ep-xxx.neon.tech/neondb`
   - **保存好这个连接串**，后面会用到

### 步骤 2：初始化数据库表结构

在 Neon 控制台执行建表脚本：

1. 左侧菜单选 **SQL Editor**
2. 打开项目中的 `db/init.sql` 文件，把全部内容复制粘贴到 SQL Editor
3. 点击 **Run** 执行
4. 执行成功后，左侧 **Tables** 中应能看到 7 张表：`surveys`、`parts`、`questions`、`question_options`、`respondents`、`responses`、`response_selected_options`

### 步骤 3：部署到 Vercel

1. 访问 https://vercel.com ，用 GitHub 登录
2. 点击 **New Project** → **Import Git Repository**
3. 选择 `gz-ebike-survey` 仓库
4. Branch 选择 `db-rds-refactor`（或你的主分支）
5. 直接点击 **Deploy**，等待部署完成（约 2-3 分钟）

### 步骤 4：配置环境变量

部署完成后，配置数据库连接和管理员密钥：

1. 进入 Vercel 项目 → **Settings** → **Environment Variables**
2. 逐个添加以下变量（Production / Preview / Development 都勾选）：

   | 变量名 | 值 | 必填 | 说明 |
   |--------|-----|------|------|
   | `POSTGRES_URL` | Neon 的完整连接串 | ✅ | PostgreSQL 数据库连接地址 |
   | `ADMIN_PASSWORD` | （自定义管理员密码） | ✅ | 管理员面板登录密码 |
   | `JWT_SECRET` | （任意长随机字符串，至少32位） | ✅ | Token 签名密钥 |
   | `JSONBIN_BIN_ID` | （旧 JSONBin 的 Bin ID） | ⚠️ 迁移时需要 | 仅迁移历史数据时填写 |
   | `JSONBIN_ACCESS_KEY` | （旧 JSONBin 的 Access Key） | ⚠️ 迁移时需要 | 仅迁移历史数据时填写 |

3. 添加完所有变量后，触发重新部署：
   - 进入最新一次 Deployment → 右上角 **Redeploy**
   - 等待部署完成

### 步骤 5：初始化题目数据

部署完成后，通过 API 接口将 `questions.json` 中的题目导入数据库。

**方式 A：用浏览器开发者工具（最简单）**

1. 打开你的网站，进入管理后台并登录
2. 按 F12 打开开发者工具，切换到 **Console** 标签
3. 粘贴以下代码并回车（先替换 `你的token` 为登录后获取的 token）：

```javascript
fetch('/api/admin/seed', {
  method: 'POST',
  headers: { 'x-admin-token': '你的token' }
}).then(r => r.json()).then(console.log)
```

4. 看到 `{ success: true, message: "Seed completed successfully", ... }` 即成功

**方式 B：用 curl 命令**

```bash
curl -X POST https://你的域名/api/admin/seed \
  -H "x-admin-token: 你的token"
```

> 如果看到 `skipped: true`，说明该版本题目已经导入过了，无需重复导入。

### 步骤 6（可选）：迁移历史答卷数据

如果之前用 JSONBin 存储了答卷，可以一键迁移到新数据库：

1. 确保环境变量中已配置 `JSONBIN_BIN_ID` 和 `JSONBIN_ACCESS_KEY`
2. 同样在浏览器 Console 中执行：

```javascript
fetch('/api/admin/migrate', {
  method: 'POST',
  headers: { 'x-admin-token': '你的token' }
}).then(r => r.json()).then(console.log)
```

返回结果会显示迁移了多少条记录。

### 验证部署

完成以上步骤后，验证是否正常：

1. 访问 `https://你的域名/api/questions` → 应返回题目 JSON 数据
2. 访问管理后台登录 → 题目管理标签页应能看到题目列表
3. 提交一份测试问卷 → 统计页应能看到数据

---

## API 接口列表

| 接口 | 方法 | 功能 | 权限 |
|------|------|------|------|
| `/api/questions` | GET | 获取问卷题目结构 | 公开 |
| `/api/auth` | POST | 管理员密码验证，返回临时 token | 公开 |
| `/api/submit` | POST | 提交问卷答案到数据库 | 公开 |
| `/api/stats` | GET | 获取统计数据 | 需 token |
| `/api/export` | GET | 导出原始数据（支持 JSON/CSV） | 需 token |
| `/api/admin/seed` | POST | 初始化题目数据（从 questions.json 导入） | 需 token |
| `/api/admin/migrate` | POST | 从 JSONBin 迁移历史答卷数据 | 需 token |
| `/api/admin/questions` | GET / POST | 题目列表查询 / 新增题目 | 需 token |
| `/api/admin/questions/[id]` | GET / PUT / DELETE | 单题详情 / 更新 / 删除 | 需 token |

---

## 安全设计

- 所有密钥存储在 Vercel 环境变量中，前端代码不含任何敏感信息
- 管理员 token 使用 HMAC 签名，1 小时自动过期
- 所有数据库查询使用参数化查询，防止 SQL 注入
- 提交接口带 IP 限流（每 IP 每分钟最多 3 次）
- 管理类接口（seed、migrate、题目 CRUD）均需管理员 token 鉴权

---

## Neon 免费版注意事项

- **冷启动**：长时间无请求后数据库会自动休眠，首次请求可能延迟 2-5 秒，之后恢复正常
- **连接数限制**：免费版有连接数上限，代码中连接池 `max: 10` 足够使用
- **闲置休眠**：5 分钟无连接会休眠，如需保持活跃可用 UptimeRobot 等定时请求
- **存储限额**：免费版 0.5 GB 存储空间，问卷数据量小的场景完全够用

---

## 分支说明

| 分支 | 说明 | 后端 | 数据库 |
|------|------|------|--------|
| `main` | 纯前端版本，数据存 localStorage + JSONBin 直连 | 无 | JSONBin |
| `feature/serverless` | Vercel Serverless 旧版，密钥在服务端 | 有（/api/*） | JSONBin |
| `db-rds-refactor` | PostgreSQL 重构版，含题目管理后台 | 有（/api/*） | PostgreSQL |

---

## 微信群分享文案

```
🛵 广州电动车管理，您怎么看？

广州电动车已超650万辆，新规已实施，"是否应该放宽限制"引发热议。

我们正在收集市民真实意见，将形成公开调研报告。
⏱️ 只需5-6分钟，19道选择题
🔒 匿名填写，严格保密

👇 点击链接参与：
https://gz-ebike-survey.vercel.app

感谢转发扩散！
```

---

## 常见问题

### 微信内无法打开链接？

这是微信对外部链接的安全拦截。解决方案：

1. 点击拦截页面的"申请恢复访问"提交申诉
2. 绑定自定义域名并完成 ICP 备案（彻底解决）
3. 在电脑浏览器或手机 Safari/Chrome 中打开（不受影响）

### Neon 数据库休眠导致首次访问慢？

Neon 免费版在 5 分钟无连接后会自动休眠，首次访问需要 2-5 秒冷启动。解决方案：

1. 使用定时任务（如 UptimeRobot）每 4-5 分钟请求一次网站，保持数据库活跃
2. 升级 Neon 付费版，可配置更长的闲置超时或 Pro plan 不休眠
3. 在前端增加加载提示，优化用户体验

### 数据库连接串泄露了怎么办？

立即在 Neon 控制台重置密码：

1. 进入 Neon 项目 → **Settings** → **Roles**
2. 找到对应的角色 → 点击 "Reset password"
3. 更新 Vercel 环境变量中的 `POSTGRES_URL`
4. 重新部署 Vercel 项目

### 如何备份数据库？

Neon 自带 PITR（时间点恢复）功能：
- 免费版：保留 1 天历史
- Pro 版：保留 30 天历史

也可以手动导出（需要本地装 psql）：

```bash
pg_dump "$POSTGRES_URL" > backup_$(date +%Y%m%d).sql
```

### 如何切换部署平台？

后端 API 使用统一路径 `/api/*`，前端代码无需修改。如需切换到其他云平台，只需重写对应的后端文件即可。
