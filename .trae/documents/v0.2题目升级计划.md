# v0.2 题目升级实施计划

## Summary

基于 preview 分支的新版问卷（v0.2），更新 questions.json、survey.html 中的硬编码引用、调研报告渲染逻辑、版本号递增。涉及新增 Q2b、删除 Q8/Q9/Q10旧/Q11旧、替换 Q10新/Q11新、调整 Q4/Q5/Q13b/Q13c/Q16 选项和类型，以及重构可行性指数算法。

## 改动文件
- `/workspace/questions.json` — 完整重写题目，版本号升为 v0.2
- `/workspace/survey.html` — 更新所有硬编码引用和报告渲染逻辑

---

## 第一部分：questions.json 更新

将 preview 分支的 `广州电动车管理调研问卷_新版预览.md` 转换为 JSON 格式。

### 结构变化
- version: "v0.1" → "v0.2"
- 新增 Q2b（checkbox，多交通工具）
- 删除 Q8（安全威胁）、Q9（充电满意度）、Q10旧（知晓率）、Q11旧（执法力度）
- 新增 Q10新（checkbox_limit maxSelect:3，管理目标）、Q11新（radio，实施路径）
- Q4 增加"货运/拉货工作""其他职业用途"
- Q5 从4选项改为5选项（城中村单列、流动居住）
- Q13b 从8选项改为10选项（checkbox_limit maxSelect:4）
- Q13c 从8选项改为11选项（checkbox_limit maxSelect:4）
- Q16 从6选项改为7选项（文案口语化，删除"不确定"，新增"全面禁止"）
- 总题数：18 → 17

---

## 第二部分：survey.html 硬编码更新

### 2.1 题目选项文字硬编码（6处）

| 行号 | 当前硬编码 | v0.2 变化 | 操作 |
|------|-----------|-----------|------|
| 1373 | `answers['q1'] === '否'` | 不变 | 无需改 |
| 1711, 1890 | `d.answers.q2 === '拥有并经常使用' \|\| d.answers.q2 === '拥有但不常用'` | 不变 | 无需改 |
| 1840 | `c.label.includes('过于严格')` | v0.2 Q12选项文字不变 | 无需改 |
| 1841 | `c.label.includes('解禁') \|\| c.label.includes('放宽')` | v0.2 Q16选项改为口语化"多开路+共享车道""全面放开" | **需改为** `c.label.includes('放开') \|\| c.label.includes('多开路') \|\| c.label.includes('分类管')` |

### 2.2 renderChapter3 — 删除 Q8/Q9 段落

当前 renderChapter3 包含 Q6/Q7/Q8/Q9 四个小节。v0.2 删除了 Q8 和 Q9，需要移除：
- 删除 "3.3 安全威胁感知" 整个段落（Q8）
- 删除 "3.4 停放充电满意度" 整个段落（Q9）
- 章节标题改为 `第三章 现状与问题感知`（去掉"核心发现一"前缀，因为内容少了）

### 2.3 renderChapter4 — 全部重写

当前 renderChapter4 是"政策认知与评价"（Q10知晓率 + Q11执法力度）。v0.2 完全替换为"管理目标与优先级"（Q10管理目标排序 + Q11实施路径）。

**新 renderChapter4 内容**：
- 4.1 管理目标优先级（Q10，checkbox_limit maxSelect:3，降序排列条形图）
  - 显示大数字卡片：最高票目标及其比例
  - 条形图按选择率降序
- 4.2 改革实施路径偏好（Q11，radio，环形/条形图）
  - 显示最多人选的路径

### 2.4 renderChapter5 — 更新标题和选项引用

- 章节标题：`第五章 "解禁"态度与条件` → `第五章 管理措施与态度`
- 5.2a/5.2b：标题可保持"希望放宽/加强管控的方面"
- 5.6 互斥注释：Q17 的 mutuallyExclusive 不变（v0.2 Q17 无变化）

### 2.5 renderChapter6 — 可行性指数重新设计

当前算法依赖 Q8/Q9/Q11旧，这些在 v0.2 中不存在。需要重新设计：

**新四维指数**：

| 维度 | 权重 | 数据来源 | 计算方式 |
|------|------|---------|---------|
| 放宽需求 | 30% | Q12 + Q16 | Q12"过于严格"比例 × 50% + Q16选择"全面放开/多开路/分类管"比例 × 50% |
| 主动优化意愿 | 25% | Q10 | Q10中选择"方便市民出行"+"促进就业民生"的比例 |
| 安全风险可控 | 25% | Q15 | Q15选"可能不会"+"一定不会"比例（不变） |
| 实施条件成熟度 | 20% | Q11 + Q13c | Q11选"先试点/分步/小步快跑"的比例 × 50% + Q13c中选择"电子号牌"+"考牌"的比例 × 50% |

结论映射规则保持不变（80+/60-79/40-59/0-39）。

**6.2 分群体差异**：车主 vs 非车主对比 Q16 不变。

**6.3 政策建议**：更新建议生成逻辑，基于新 Q10/Q11 数据：
- 建议基于 Q10 最高优先级目标
- 建议基于 Q11 最受欢迎的改革路径
- 建议基于可行性指数

### 2.6 renderChapter7 — 无需改

Q18 不变。

### 2.7 landingSurveyInfo 文字

当前：`只需5分钟，${TOTAL_QUESTIONS}道选择题`
v0.2 总题数 17 → 会自动显示正确数字，但 MD 文件标注预估时长为 5-6分钟，不改。

### 2.8 renderStatsWithData — isFeatured 逻辑

当前：`q.id === 'q16'` 标记为核心结论题
v0.2：Q16 仍是核心结论题，不需要改 ID。

但 `q.id === 'q13'` 的特殊处理需要检查。当前代码在 likert 类型判断时 `if (isFeatured || q.id === 'q13')` 用 featured 样式。v0.2 中 Q13 不存在了（只有 Q13b 和 Q13c），但这是在 `questions.forEach` 遍历中，Q13b/Q13c 会被正确处理为 checkbox/checkbox_limit 类型，不会进入 likert 分支。所以 `q.id === 'q13'` 永远不会匹配到，不会出错。**但为了代码整洁，应删除 `q.id === 'q13'` 的特殊处理。**

### 2.9 互斥逻辑确认

Q17 的 mutuallyExclusive: [6, 7] 不变，前端互斥处理逻辑无需修改。

### 2.10 doStartSurvey — visibleQuestions

当前：`visibleQuestions = [...questions]` 直接复制全部。v0.2 新增 Q2b 后，Q2b 会被自动包含在遍历中，无需手动插入。Q8/Q9/Q10旧/Q11旧 删除后也不会出现在遍历中。**无需改动。**

### 2.11 index.html 同步

完成后 `cp survey.html index.html`。

---

## 第三部分：版本号处理

按照约定，题目修改 → 版本号自增：
- 主版本不变（不升v1.0，因为不是正式发布）
- questions.json 中 `"version": "v0.1"` → `"version": "v0.2"`

---

## Assumptions

1. 不修改 preview 分支，只在 main 分支上操作
2. questions.json 中的新 Q10 新 Q11 使用旧题号（保持向后兼容），但 ID 仍为 q10/q11
3. v0.2 的 Q2b ID 设为 "q2b"
4. 前端互斥逻辑（mutuallyExclusive）不变，Q17 的 [6,7] 索引对应新版选项不变
5. 旧版 v0.1 的数据（JSONBin 中已有记录）在报告中仍然可正常显示（renderStatsWithData 逐题渲染旧数据时不依赖不存在的 Q8/Q9 等新 ID）

## Verification Steps

1. JS 语法验证（node -e 解析）
2. 新 questions.json 格式验证（JSON.parse 无报错）
3. 确认所有 renderChapter 函数引用的 q ID 在新 questions.json 中存在
4. 确认所有选项文字硬编码匹配新选项
5. 确认 Q17 mutuallyExclusive 索引对应正确
6. 确认可行性指数计算不会除以零
7. cp survey.html index.html + git commit + git push
