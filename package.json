{
  "name": "gz-ebike-survey",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "db:init": "psql $POSTGRES_URL -f db/init.sql",
    "db:seed": "node db/seed.js",
    "db:migrate": "node db/migrate-data.js"
  },
  "dependencies": {
    "pg": "^8.11.3"
  }
}
